import { ReactElement } from "react";
import { IContainer } from "..";
declare const Container: {
    ({ children, root }: IContainer): ReactElement;
    displayName: string;
};
export default Container;
