import { ReactElement } from "react";
import { ITableHeader } from "../..";
declare const TableHeader: {
    (props: ITableHeader): ReactElement;
    displayName: string;
};
export default TableHeader;
