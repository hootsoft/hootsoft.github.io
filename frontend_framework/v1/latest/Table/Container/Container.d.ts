import { ReactElement } from "react";
import { ITableContainer } from "../..";
declare const TableContainer: {
    ({ children, dense }: ITableContainer): ReactElement;
    displayName: string;
};
export default TableContainer;
