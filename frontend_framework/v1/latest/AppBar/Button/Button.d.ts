import { ReactElement } from "react";
import { IAppBarButton } from "../..";
declare const AppBarButton: {
    ({ "aria-label": label, children, className, onClick, text }: IAppBarButton): ReactElement;
    displayName: string;
};
export default AppBarButton;
