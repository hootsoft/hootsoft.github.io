import { ReactElement } from "react";
import { IAlertContent } from "../..";
declare const AlertContent: {
    ({ bodyText, children, headerText }: IAlertContent): ReactElement;
    displayName: string;
};
export default AlertContent;
