/**
 * Registers a stylesheet when the component mounts, adding it to the head element. Revokes the
 * style when all components using the style are unmounted.
 * @param css The css value.
 */
export default function useStyle(css: any): void;
